export enum ModMailThreadStatus {
  OPEN = "open",
  CLOSED = "closed",
  SUSPENDED = "suspended",
}
export enum ModMailMessageType {
  USER = "user",
  STAFF = "staff",
  CLIENT = "client",
}
export enum ModMailMessageSentTo {
  USER = "user",
  THREAD = "thread",
}
